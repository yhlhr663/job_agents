[server]
# Lets the app serve files placed in ./static/ at the URL path app/static/<file>,
# so saved resume PDFs can be opened in the browser (file:// links are blocked by Chrome).
enableStaticServing = true
